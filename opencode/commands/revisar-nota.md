---
description: Lee y analiza una nota de Joplin por su ID
agent: build
subtask: true
---
Lee la nota con ID $1 usando la herramienta `Joplin_read_note` en la libreta `WiKi_LLM`.  
Si no se proporciona un ID, solicítalo al usuario.  

Muestra la siguiente información:  
- Título de la nota  
- Contenido completo  
- Etiquetas (tags)  

Si la nota no existe, indica claramente que no se encontró.
